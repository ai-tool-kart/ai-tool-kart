/* Placeholder — the real catalog lands in Phase 5. */
export default function BrowsePage() {
  return (
    <section className="mx-auto max-w-site px-8 pt-16">
      <h1 className="text-[40px] font-bold tracking-[-0.032em] text-ink">Browse</h1>
      <p className="mt-3 text-[15px] text-muted-dim">Filter sidebar, toolbar and result grid arrive in Phase 5.</p>
    </section>
  )
}
