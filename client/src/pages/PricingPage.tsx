/* Placeholder — the real tiers and feature matrix land in Phase 7. */
export default function PricingPage() {
  return (
    <section className="mx-auto max-w-site px-8 pt-16">
      <h1 className="text-[40px] font-bold tracking-[-0.032em] text-ink">Pricing</h1>
      <p className="mt-3 text-[15px] text-muted-dim">Billing toggle, tier cards and the feature matrix arrive in Phase 7.</p>
    </section>
  )
}
