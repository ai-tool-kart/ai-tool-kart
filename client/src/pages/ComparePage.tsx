/* Placeholder — the real comparison table lands in Phase 6. */
export default function ComparePage() {
  return (
    <section className="mx-auto max-w-site px-8 pt-16">
      <h1 className="text-[40px] font-bold tracking-[-0.032em] text-ink">Compare</h1>
      <p className="mt-3 text-[15px] text-muted-dim">Tool pickers and the comparison table arrive in Phase 6.</p>
    </section>
  )
}
